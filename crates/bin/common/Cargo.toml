[package]
name = "common"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
gpu 	= { path = "../../libs/gpu" }
math 	= { path = "../../libs/math" }

log = "*"
rand = "*"
raw-window-handle = "*"
glsl-to-spirv = "*"
image = "0.24.2"
